package wairoadc.godiscover.controller;

import java.util.ArrayList;

import wairoadc.godiscover.model.Track;
import wairoadc.godiscover.model.Type;

/**
 * Created by Lucas on 7/01/2015.
 */

// Controller that assists in tracking the progress of users.
public class ProgressController {

    // Creates the overall progress of a track on a list, position 0 contains the total and position 1 contains the progress.
    public ArrayList<Integer> createOverallProgress(Track track) {
        return null;
    }

    // Creates the progress of a type of resource on a track, position 0 contains the total and position 1 contains the progress.
    public ArrayList<Integer> createProgressByType(Track track, Type type) {
        return null;
    }

    // Creates the progress of spots on a track, position 0 contains the total and position 1 contains the progress.
    public ArrayList<Integer> createProgressSpot(Track track) {
        return null;
    }
}
